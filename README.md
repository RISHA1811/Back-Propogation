# Back-Propogation
Backpropagation is a supervised learning algorithm used to train neural networks by computing gradients of the loss function using the chain rule. It updates weights and biases through gradient descent to minimize prediction error and improve model performance.
